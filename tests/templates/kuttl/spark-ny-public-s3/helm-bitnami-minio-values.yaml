---
volumePermissions:
  enabled: false

podSecurityContext:
  enabled: false

containerSecurityContext:
  enabled: false

mode: standalone

disableWebUI: true

persistence:
  enabled: false

auth:
  existingSecret: minio-credentials

resources:
  requests:
    memory: 1Gi
